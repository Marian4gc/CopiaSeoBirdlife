{% extends 'base.html.twig' %}

{% block title %}Coordenadas{% endblock %}

{% block body %}
<div class="title-image-container1">
     <img src="{{ asset('images/petirojoIzq.png') }}" alt="Birds Image">
    <h1>Coordenadas</h1>
</div>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coordenada.id }}</td>
            </tr>
            <tr>
                <th>Longitud</th>
                <td>{{ coordenada.longitud }}</td>
            </tr>
            <tr>
                <th>Latitud</th>
                <td>{{ coordenada.latitud }}</td>
            </tr>
        </tbody>
    </table>

    <a type="button" class="btn btn-info" href="{{ path('app_coordenadas_index') }}">Back to list</a>

    <a type="button" class="btn btn-dark" href="{{ path('app_coordenadas_edit', {'id': coordenada.id}) }}">Edit</a>

    {{ include('coordenadas/_delete_form.html.twig') }}
{% endblock %}
