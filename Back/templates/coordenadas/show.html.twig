{% extends 'base.html.twig' %}

{% block title %}Coordenadas{% endblock %}

{% block body %}
    <h1>Coordenadas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coordenada.id }}</td>
            </tr>
            <tr>
                <th>Longitud</th>
                <td>{{ coordenada.longitud }}</td>
            </tr>
            <tr>
                <th>Latitud</th>
                <td>{{ coordenada.latitud }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_coordenadas_index') }}">back to list</a>

    <a href="{{ path('app_coordenadas_edit', {'id': coordenada.id}) }}">edit</a>

    {{ include('coordenadas/_delete_form.html.twig') }}
{% endblock %}
