{% extends 'base.html.twig' %}

{% block title %}Totaldata{% endblock %}

{% block body %}
    <h1>Totaldata</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ totaldatum.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_totaldata_index') }}">back to list</a>

    <a href="{{ path('app_totaldata_edit', {'id': totaldatum.id}) }}">edit</a>

    {{ include('totaldata/_delete_form.html.twig') }}
{% endblock %}
