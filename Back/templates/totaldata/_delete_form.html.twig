<form method="post" action="{{ path('app_totaldata_delete', {'id': totaldatum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ totaldatum.id) }}">
    <button class="btn">Delete</button>
</form>
