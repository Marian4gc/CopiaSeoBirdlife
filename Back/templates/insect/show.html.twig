{% extends 'base.html.twig' %}

{% block title %}Insect{% endblock %}

{% block body %}
    <h1>Insect</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ insect.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ insect.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ insect.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ insect.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_insect_index') }}">back to list</a>

    <a href="{{ path('app_insect_edit', {'id': insect.id}) }}">edit</a>

    {{ include('insect/_delete_form.html.twig') }}
{% endblock %}
