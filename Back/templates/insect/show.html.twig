{% extends 'base.html.twig' %}

{% block title %}Insect{% endblock %}

{% block body %}
<div class="title-image-container2">
    <img src="{{ asset('images/butterfly.svg') }}" alt="mariposa Image"
    <h1>Insect</h1>

</div>
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ insect.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ insect.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ insect.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ insect.image }}</td>
            </tr>
        </tbody>
    </table>

    <a type="button" class="btn btn-info" href="{{ path('app_insect_index') }}">back to list</a>

    <a type="button" class="btn btn-dark"  href="{{ path('app_insect_edit', {'id': insect.id}) }}">edit</a>

    {{ include('insect/_delete_form.html.twig') }}
{% endblock %}
