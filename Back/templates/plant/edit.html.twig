{% extends 'base.html.twig' %}

{% block title %}Edit Plant{% endblock %}

{% block body %}
    <div class="title-image-container3">
        <img src="{{ asset('images/plant.svg') }}" alt="plant Image">
        <h1>Edit Plant</h1>
    </div>

    {{ include('plant/_form.html.twig', {'button_label': 'Update'}) }}

    <a type="button" class="btn btn-info" href="{{ path('app_plant_index') }}">back to list</a>

    {{ include('plant/_delete_form.html.twig') }}
{% endblock %}
